export const add = (a, b) => a + b
export const first = (arr) => arr[0]
export const last = (arr) => arr[arr.length - 1]
export const { values, keys, entries } = Object

export const getId = () => Math.round(Math.random() * 9999999)
